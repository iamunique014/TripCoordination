@* _Sidebar.cshtml *@

@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <div class="d-flex flex-column flex-shrink-0 p-3 bg-warning border-end min-vh-100" style="width: 250px;">
        <h5 class="mb-3">Dashboard</h5>
        <ul class="nav nav-pills flex-column mb-auto">
            @if (User.IsInRole("Admin"))
            {
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Trip" asp-action="Create">Create Trip</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Trip" asp-action="Index">All Trips</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="User" asp-action="Manage">Manage Users</a>
                </li>
            }
            else if (User.IsInRole("Organizer"))
            {
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Trip" asp-action="MyTrips">My Trips</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Trip" asp-action="Create">Create Trip</a>
                </li>
            }
            else if (User.IsInRole("Student"))
            {
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Trip" asp-action="Booking">Book Trip</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Trip" asp-action="MyTrips">My Trips</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Profile" asp-action="Index">Profile</a>
                </li>
            }
            else if (User.IsInRole("User"))
            {
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Trip" asp-action="Search">Search Trips</a>
                </li>
            }
        </ul>
    </div>
}


@* <div class="menu rounded">
    @if (User.IsInRole("Admin")){
        <a href="@Url.Action("AdminDashboard", "Admin")">
            Dashboard
        </a>
        <a href="@Url.Action("ManageResidences", "Admin")">
            Residences
        </a>
        <a href="@Url.Action("ManageTowns", "Admin")">
            Towns
        </a>
        <a href="@Url.Action("ManageTrips", "Admin")">
            Trips
        </a>
        <a href="@Url.Action("ManageUsers", "Admin")">
            Users
        </a>
        <a href="@Url.Action("EditProfile", "Profile")">
            Profile
        </a>
    }

    @if (User.IsInRole("Organizer"))
    {
        <a href="@Url.Action("OrganizerDashboard", "Organizer")">
            Dashboard
        </a>
        <a href="@Url.Action("ManageUsers", "Admin")">
            Users
        </a>
        <a href="@Url.Action("EditProfile", "Profile")">
            Profile
        </a>
    }

   
</div> *@













































@* <style>
    .menu {
        position: fixed;
        width: 250px;
        background-color: #f4f4f4;
        padding: 15px;
        box-shadow: 2px 0 5px rgba(0,0,0,0.1); 
    }

    .menu h3 {
        margin-bottom: 20px;
    }

    .menu a {
        display: flex;
        align-items: center;
        color: #333;
        text-decoration: none;
        padding: 10px;
        margin-bottom: 5px;
        border-radius: 4px;
        transition: background-color 0.3s;
    }

    .menu a:hover {
        background-color: cadetblue;
        color: black;
    }


</style> *@